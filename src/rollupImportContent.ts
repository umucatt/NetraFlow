export const ROLLUP_IMPORT_EXPLANATION = `### 1. 这段提示词的用途

这段提示词用于让外部 AI 根据你提供的账单、截图、表格、PDF、CSV、XLSX 或资产记录，生成 NetraFlow 可快速导入的标准汇总文件。

生成结果会被整理为按日期汇总的账户变动记录，而不是原始材料中的逐条明细。

---

### 2. 生成结果的两种类型

整理结果主要分为两类：

change

用于交易流水、收入支出、支付账单等材料。

它表示：

某一天的净变动合计

balance

用于余额、净值、市值、期末资产等材料。

它表示：

某一天最后看到的余额或资产值

---

### 3. 为什么按汇总记录处理

NetraFlow 写入的是账户历史修改记录，而不是原始账单中的逐条交易。

这样可以避免导入内容变成传统账本明细，也能让历史记录保持克制、清晰。

---

### 4. 日期与金额方向

日期和金额方向是整理过程中最容易出错的部分。

因此，提示词会要求外部 AI 优先使用材料中明确出现的信息。

金额方向应优先依据：

正负号
收入 / 支出字段
转入 / 转出标识
借方 / 贷方方向

日期应尽量依据：

完整日期
材料日期
截图时间
文件名
账期
上下文

如果截图未包含明确日期，最好具有时效性。这样外部 AI 更容易正确理解“今天”“昨天”等相对日期。

---

### 5. 材料基准日期与账户关键词

材料基准日期用于帮助外部 AI 判断相对日期。

例如：

今天
昨天
周三

材料来源 / 账户关键词用于说明这份材料大致来自哪里。

例如：

支付宝
微信支付
银行流水
基金月报
信用卡账单

这些信息只是辅助判断线索。

理想情况下，用户复制提示词到外部 AI 后，同时上传截图或文件，就可以开始整理；如果材料本身已经包含清楚的日期和来源，通常不需要额外填写太多内容。

---

### 6. 账户关键词的含义

账户关键词不等同于 NetraFlow 中的最终导入账户。

它只表示：

这份材料大致来自哪里

导入 NetraFlow 后，用户仍需在本地确认这些汇总记录写入哪个具体账户。

---

### 7. 重复计算规避

提示词会要求外部 AI 区分：

可按日期归属的明细内容
材料中的概览信息

如果同一份材料同时包含明细和汇总，整理时应避免把同一批金额重复写入导入结果。

---

### 8. 输出形式

外部 AI 最终应输出合法 JSON，或导出为汇总文件。

推荐文件名：

netraflow_rollup_YYYYMMDD_HHMM.json

如果外部 AI 无法直接生成文件，也可以直接输出 JSON 文本，由用户复制后手动导入 NetraFlow。

---

### 9. NetraFlow 的本地边界

NetraFlow 是本地离线工具，不内置 AI 或识别模型，也不会连接外部平台。

导入时，NetraFlow 只能检查：

汇总文件格式
字段结构
部分本地规则

NetraFlow 无法验证外部整理结果的数据准确性。

导入页面显示的风险等级，只表示 NetraFlow 是否发现明显格式或结构问题，不代表结果已经被证明正确。`;

export const ROLLUP_IMPORT_PROMPT = `请把我提供的账单截图、流水表格、PDF 账单文字、CSV/XLSX 内容、基金账户记录或其他资产变动材料，整理成按日期汇总的个人本地记账 / 资产记录数据。

本任务仅用于个人本地记录整理，不用于金融分析。

你不需要、也不要：
1. 分析财务状况
2. 评价消费行为
3. 提供投资建议
4. 推荐理财产品
5. 进行风险评估
6. 预测收益
7. 生成预算、分类统计、图表或报告

你的任务只有一个：
从材料中提取可见金额信息，并按日期汇总成结构化记录。

请按以下顺序处理，但不要输出处理过程：

1. 先识别所有日期分组，例如今天、昨天、具体日期、周几等。
2. 再把每条有效非零金额记录归入对应日期。
3. 每个日期分别计算正数合计、负数合计和净变动。
4. 最后只输出按日期汇总后的 JSON。

材料信息：

材料基准日期：
YYYY-MM-DD

材料来源 / 账户关键词：
例如：支付宝、微信支付、招商银行流水、基金月报、信用卡账单

如果材料基准日期没有填写，请根据材料中的截图日期、导出日期、账期、文件名、页面时间或当前上下文日期判断。

如果材料来源 / 账户关键词已经填写，accountKeyword 优先使用这里填写的内容。

重要原则：

1. 不要逐笔输出交易。
2. 不要输出消费类别。
3. 不要输出备注。
4. 不要生成明细账。
5. 只生成按日期汇总后的修改记录。
6. 只处理能够确认已经影响账户金额的有效金额记录。
7. 如果某项内容只是状态提示、展示信息、占位金额、待确认事项、处理中事项、优惠说明、抵扣说明，或金额实际为零，且不能确认它已经造成账户金额变化，则不要放入 records。
8. 无法可靠整理成正式记录的内容，可以放入 unresolvedItems。

整理模式：

1. balance

用于余额、净值、市值、期末资产、当前金额类内容。

规则：
- 同一天、同一账户关键词，只保留当天最后一个余额 / 净值 / 市值 / 期末金额。
- 如果是月报、账期报告、期末资产记录，可以使用该周期最后一天作为日期。
- 如果同一材料中同时出现期末余额 / 市值和期间收益 / 变动，优先输出 balance，不要同时输出收益 change，避免重复计算。

示例：
“2026年4月期末资产 21350”
应整理为：
date = 2026-04-30
mode = balance
amount = 21350

2. change

用于交易流水、账单明细、收入支出记录。

规则：
- 同一天、同一账户关键词，只输出一条净变动合计。
- 不要逐条列出消费、收入、转账等明细。
- 如果某一天没有可汇总的明确金额变化，则不生成正式记录。

示例：
某天有：
餐饮 -32
打车 -18
退款 +10
工资 +5000

应整理为一条：
date = 当天日期
mode = change
amount = 4960

金额方向判断规则：

1. 优先使用原始材料中的明确方向。

如果原始内容中存在以下信息，必须优先采用：
- 明确的 + / - 符号
- 收入 / 支出字段
- 收入金额 / 支出金额分列
- 转入 / 转出标识
- 借方 / 贷方方向
- 账单中明确标注的增加 / 减少

不要在已有明确方向的情况下，仅凭标题、商户名或交易描述重新推断方向。

2. 只有当原始材料没有明确方向时，才使用语义辅助判断。

通常记为正数：
收入、到账、收款、退款、返现、转入、充值、收益增加

通常记为负数：
消费、付款、扣款、支出、转出、提现、手续费

3. 如果仍然无法判断金额方向，不要强行放入 records。
可以放入 unresolvedItems。

日期规则：

1. records 中的 date 必须使用 YYYY-MM-DD。
2. 如果材料中有完整日期，直接使用完整日期。
3. 如果材料中只有相对日期或不完整日期，可以根据材料标题、账期、文件名、截图日期、导出日期、用户提供的材料日期，或当前上下文日期进行换算。
4. 默认认为用户提供的材料具有时效性：
   - “今天”按材料日期或当前日期处理
   - “昨天”按材料日期或当前日期的前一天处理
   - “前天”按材料日期或当前日期的前两天处理
5. 如果材料中出现“周一 / 周二 / 周三”等星期型日期：
   - 按“不晚于材料基准日期的最近一次对应星期”解析。
   - 不要解析成未来日期。
   - 例如材料基准日期是周一，材料中显示“周三”，应理解为上周三。
6. 如果列表中存在分组标题、月份标题、上下文日期、相邻记录日期或明显的时间排序，应结合这些信息校正日期。
7. 只有在材料明显过期、时间信息互相冲突、无法确定基准日期，或日期推断会影响金额归属时，才放入 unresolvedItems。
8. 不要编造与材料无关的日期。

汇总与重复计算规则：

1. 如果材料中同时存在明细记录和周期汇总 / 页面总览 / 分类统计 / 合计行，应避免重复计算。
2. 优先使用能明确归属到日期的明细记录进行按日汇总。
3. 周期汇总、页面总览、分类统计、收入支出总览、合计行等内容，通常只作为核对参考，不应与明细同时生成 records。
4. 只有在没有可用明细，且汇总信息本身能可靠转换为某个日期或周期的记录时，才可以整理为 records。
5. 如果无法可靠转换为具体日期或周期记录，放入 unresolvedItems。

accountKeyword 规则：

accountKeyword 是账户关键词 / 来源关键词，用于表示这份材料大致来自哪里。

填写优先级：

1. 如果用户在“材料来源 / 账户关键词”中填写了内容，优先使用该内容。
2. 如果材料中有明确的平台、账户、账单主体、页面标题、文件名来源或截图所属 App，可使用这些信息。
3. 如果整份材料明显来自同一个账单页面、账户页面或平台，使用该材料来源作为 accountKeyword。
4. 如果仍然无法判断，可以填写空字符串。

accountKeyword 应表示“整份材料的来源容器”，而不是“单条明细的内容”。

不要把单条明细中的交易对象、分类、商品、活动、备注、标题、对手方或用途说明当作 accountKeyword。

导出要求：

请优先导出为 JSON 文件。

文件名建议使用：

netraflow_rollup_YYYYMMDD_HHMM.json

例如：

netraflow_rollup_20260504_1530.json

不要生成图片。
不要生成 PDF。
不要生成 Word 文档。
不要生成图表。
不要生成分析报告。

如果无法直接生成文件，请把完整 JSON 内容以纯文本形式输出在对话框中，方便复制。

输出必须是合法 JSON。
不要输出 Markdown。
不要输出解释说明。
不要添加 JSON 以外的文字。

JSON 格式如下：

{
  "format": "netraflow_rollup",
  "records": [
    {
      "date": "YYYY-MM-DD",
      "mode": "change",
      "amount": 0,
      "currency": "CNY",
      "accountKeyword": ""
    }
  ],
  "unresolvedItems": [
    {
      "reason": "",
      "dateText": "",
      "mode": "",
      "amount": null,
      "currency": "CNY",
      "accountKeyword": "",
      "rawText": ""
    }
  ]
}

字段说明：

format：
固定填写 netraflow_rollup。

records：
可以正式导入的按日汇总记录。

unresolvedItems：
无法可靠整理成正式记录的内容。
这些内容不会直接导入，只用于后续人工查看、补充或跳过。

date：
日期，格式为 YYYY-MM-DD。
只有能可靠确认完整日期时，才放入 records。

mode：
只能填写 change 或 balance。

change：
表示该日期的净变动合计。

balance：
表示该日期最后看到的余额、净值、市值或期末金额。

amount：
金额数字。
mode 为 change 时，正数表示增加，负数表示减少。
mode 为 balance 时，表示余额 / 净值 / 市值 / 期末金额。

currency：
币种。
人民币填写 CNY。
无法判断币种时可以填写空字符串。

accountKeyword：
账户关键词 / 来源关键词。
优先填写用户提供的“材料来源 / 账户关键词”；如果没有，则填写材料中可见的平台、账户、账单主体或来源线索。

reason：
unresolvedItems 中使用，说明无法正式整理的原因。

dateText：
unresolvedItems 中使用，保留原始材料中不完整或无法确认的日期文字。

rawText：
unresolvedItems 中使用，保留相关原始内容，方便后续核对。

下面是需要整理的内容：

【在这里粘贴截图识别文本、账单文本、表格内容、PDF 提取内容、CSV/XLSX 内容或资产记录】`;
